---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toLocaleDateString()}>
	{
		date.toLocaleDateString('en-us', {
            year: 'numeric',
            month: 'long',
            day: 'numeric'
		})
	}
</time>
